bind 127.0.0.1
protected-mode yes
port 6379

daemonize no
supervised no
pidfile /tmp/cognitrix-redis.pid
logfile ""

dir /root/.cognitrix/redis
appendonly yes
appendfsync everysec

# Concurrency leases must never disappear because Redis evicted their keys.
maxmemory-policy noeviction
